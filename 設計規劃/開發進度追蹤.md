# 開發進度追蹤

## 2026-07-15(下午) scanner.py 改 scan-free：正常執行不再全掃 D:\yuanyu（✅完成/已驗證）

- **問題**：前一版「F 欄皆已填才跳過全碟索引」是全工作簿 all-or-nothing；主檔 122 分頁有 36 個
  歷史舊分頁 F 欄永遠空（資料夾已不在／填不到），只要一個空就重走整個 D:\yuanyu（2 萬多目錄），每次重複。
- **決策**（使用者拍板）：只算交辦新項目，不再全掃。
- **scanner.py 改動**（v2026_0715_1330）：
  - 新增 `--reindex`：唯一會全碟走訪（build_path_index + fallback_scan）的維護模式。
  - `--main`/`--tasks` 預設 **scan-free**：不呼叫 build_path_index、也不呼叫 fallback_scan；
    F 欄用主檔已存值＋交辦路徑；新增 `fill_paths_from_tasks()` 當安全網（Claude 已填 F 時等於 no-op）。
  - 手動模式（對話框／--excel／--latest）維持原本 all-or-nothing（回歸保護）。
  - 取捨：scan-free 不再自動補收「在 D:\yuanyu 但不在 Excel」的韌體，用 `--reindex` 回復。
- **驗證**：scan-free → build_path_index/fallback_scan 皆 0 次呼叫、JSON 正常產出、清空交辦、~5 秒；
  --reindex → 兩者各 1 次；手動 --excel → 維持原走訪；fill_paths_from_tasks 實測補入正確路徑。
- **文件**：update_and_push.bat 加註、操作sop.txt、測試資料查詢工具_規劃.md 補記 scan-free 與 --reindex。

## 2026-07-15 自動化重整：交辦清單＋單一主檔＋NG 去重＋無視窗（✅程式完成/⏳待實跑與遷移）

- **背景**：導入 Claude(Cowork) 自動化時踩到 5 問題（偵測失效、重複全碟掃描、NG_Reasons 重複、
  無排除機制、bat 卡住）。使用者拍板 4 方向：交辦清單、單一主檔、NG 兩段式自動+人工確認、排除設定檔。
- **git 釐清**：確認 `Software Change Log` Excel **從未進入公開 repo 歷史**（`*.xlsx` 被 gitignore 擋住），
  機密無外洩；scanner.py 等查詢工具已公開但使用者接受，故不改寫歷史。
- **新增** `pipeline_common.py`（✅）：主檔解析 `resolve_main_excel`、備份 `backup_excel`（→`備份/`）、
  交辦 `load_tasks`/`archive_and_clear_tasks`、排除 `load_excludes`/`is_excluded`。
- **ng_prepare.py**（✅ 已驗證）：加 `--main`/`--tasks`/`--no-input`（分頁改由 `pending_tasks.txt` 推導、
  容忍分頁名空白差異）；加 `--dedup-report`/`--dedup-apply`（實測 915 列→移 47 冗餘列、0 人工內容遺失，32 組待人工檢視）。
- **scanner.py**（✅ 已驗證）：加 `--main`/`--tasks`；F 欄已填則**跳過全碟索引**（增量）；套排除；成功後清空交辦。
- **bat/文件**（✅）：`update_and_push.bat` 改 `scanner.py --main --no-input`（修卡住）；新增
  `update_and_push_scheduled.bat`（無 pause、寫 log）；更新 `操作sop.txt`、`測試資料查詢工具_規劃.md`、`CLAUDE.md`。
- **Phase F 已完成**（✅）：以 `Software Change Log_20260714_1610.xlsx`（使用者指定權威版）建立主檔
  `Software Change Log.xlsx`；跑 dedup-apply（先備份到 `備份/`，915→868、移 47 冗餘、0 人工內容遺失）；
  27 份舊 `_AI`/`_副本` 全數移入 `備份/舊副本/`（不刪除）。主檔目錄現只剩單一主檔。
- **待辦**：⚠️ scanner `--main --no-input` 完整實跑（會動網路磁碟機/覆寫 firmware_db.json，留待使用者於正式環境）。
- **影響檔案**：pipeline_common.py（新）、ng_prepare.py、scanner.py、update_and_push.bat、
  update_and_push_scheduled.bat（新）、.gitignore、操作sop.txt、測試資料查詢工具_規劃.md、CLAUDE.md。

## 2026-07-15 scanner.py 新增無視窗/無互動排程模式（⏳待完整驗證）

- **目的**：讓 `scanner.py` 可在工作排程器中自動執行，不跳 tkinter 選檔視窗、不被 `input()` 卡住。
- **新增 argparse 三參數**（不帶參數 = 完全維持原手動行為）：
  - `--excel "<路徑>"`：直接指定 Excel，不跳視窗。
  - `--latest`：自動挑選 script_dir 中符合 `Software Change Log_*.xlsx`、mtime 最新者；
    **排除檔名含 `_副本_` 的備份**，避免誤選 `make_backup()` 產物造成連鎖再處理。
  - `--no-input`：無互動模式，所有失敗改 `sys.exit(1)`、正常完成 `sys.exit(0)`，供排程判斷成敗。
- **影響檔案**：`scanner.py`
  - 新增 `import sys` / `import argparse`。
  - 新增 `find_latest_excel(script_dir)`。
  - `main()` → `main(args)`；內部 `abort()` helper 統一處理 3 個失敗點（找不到/未選檔、備份失敗、
    處理 Excel 例外）與最終成功結束。
  - `if __name__ == "__main__"` 加 argparse。
  - `__version__` 更新為 `2026_0715_0814`。
- **掃描/填路徑/解析 Define.h/輸出 firmware_db.json/複製網路磁碟機邏輯完全未動**。
- **已驗證** ✅：`python -c ast.parse` 語法通過；`--help` 列出三參數；
  `--excel <不存在路徑> --no-input` → 印錯誤且 exit code = 1（中止於備份前，無副作用）。
- **未驗證** ⚠️：完整成功流程（`--latest --no-input` 實跑）尚未執行，因會實際掃描 `D:\yuanyu`、
  產生備份、覆寫 `firmware_db.json`、複製到網路磁碟機，留待使用者於適當時機執行。
- **排程指令**：`python scanner.py --latest --no-input`
- **update_and_push.bat**：如要改用排程模式，把 line 10 的 `python scanner.py` 換成
  `python scanner.py --latest --no-input`；其下 `if errorlevel 1` 天然相容。本次未改動 bat。
